#include <stdio.h>
/**
 * main - entry point
 * description of my code: write digit numbers of base 10
 * Return: always 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
